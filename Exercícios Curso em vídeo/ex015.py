# Calculo de Reajuste Salarial

salário = float(input('Digite o salário do funcionário:'))
aumento = salário + (salário * 50 / 100)
print('O salário do funcionário é {}, com aumento de 50% é {}.'.format(salário, aumento))
